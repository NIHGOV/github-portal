name: NIH GHMP Container (Prod) - Build & Register

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'Dockerfile'
      - 'api/**'
      - 'bin/**'
      - 'business/**'
      - 'config/**'
      - 'interfaces/**'
      - 'jobs/**'
      - 'lib/**'
      - 'middleware/**'
      - 'routes/**'
      - 'typings/**'
      - 'index.ts'
      - 'job.ts'
      - 'package.json'
      - 'bun.lock'
      - 'tsconfig.json'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: get orgs from graphql
        run: |
          gh api graphql -f ent=nih -f query='query($ent: String!) {
            enterprise(slug: $ent) {
              organizations(first: 100) {
                nodes {
                  description
                  login
                  databaseId
                  id
                  name
                }
              }
            }
          }' >> raw.json
          cat raw.json
        env:
          GITHUB_TOKEN: ${{ secrets.ENT_READ_PAT}}
      - name: run powershell script to create env orgs json
        run: |
          ./.github/scripts/Create-EnvOrgs.ps1
          cat orgs.json
          ls
        shell: pwsh
      - name: check file
        run: |
          cat orgs.json
          ls -lah
          rm env-orgs.json
          cat orgs.json >> env-orgs.json
          cat env-orgs.json
      - uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ${{ secrets.PROD_REGISTRY_SERVER }}
          username: ${{ secrets.PROD_REGISTRY_USER }}
          password: ${{ secrets.PROD_REGISTRY_PASS }}
      - run: |
          SHA_TAG="${{ github.sha }}"
          docker build . \
            -t ${{ secrets.PROD_REGISTRY_SERVER }}/portal:"${SHA_TAG}" \
            -t ${{ secrets.PROD_REGISTRY_SERVER }}/portal:latest
          docker push ${{ secrets.PROD_REGISTRY_SERVER }}/portal:"${SHA_TAG}"
          docker push ${{ secrets.PROD_REGISTRY_SERVER }}/portal:latest
